//Debugging
function log(par){console.log(par)}
function error(par){console.error(par)}
function warn(par){console.warn(par)}

//Drawing------------------------------

function rect(x, y, w, h, c, s, f) { createRectAt(x, y, w, h, c, s, f) }//Hard as fuck to use